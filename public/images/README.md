# Images: drop yours here

The site auto-detects these files. Add them and they replace the placeholders
on the next reload, no code changes needed.

| File                       | Where it shows        | Notes                                  |
| -------------------------- | --------------------- | -------------------------------------- |
| `igor.jpg`                 | Hero identity card    | A square crop looks best (e.g. 600×600).|
| `kualia-screenshot.png`    | Kualia spotlight phone| Portrait phone screenshot (9:19-ish).   |
| `og.png` (in `/public`)    | Social share preview  | 1200×630 recommended.                   |

Brand logos live in `/public/logos/` as simple SVG marks. Swap in official
assets any time; keep the same filenames and they'll just work.
